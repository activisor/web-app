// `app/schedule.tsx` is the UI for the `/schedule` URL
export default function Schedule() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      <h1>Schedule Result</h1>
    </main>
  )
}